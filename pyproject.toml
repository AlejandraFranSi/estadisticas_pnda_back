[project]
name = "back"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AlejandraFranSi", email = "=" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
back = "back:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
